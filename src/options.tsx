import './options.css'
export default function Options(){
    return (
        <div style={{color:'black',fontSize:'32px',height:'100vh',fontWeight:'bold',display:'flex',justifyContent:'center',alignItems:'center'}}>
            <div style={{position:'relative',bottom:'60px'}}>不以其道，得之不去也。</div>
        </div>
    )
}